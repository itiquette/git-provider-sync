// SPDX-FileCopyrightText: 2025 The Git Provider Sync Authors
//
// SPDX-License-Identifier: CC0-1.0

= Installation Guide for Git Provider Sync
:toc: left
:toclevels: 3
:sectnums:

== Introduction
This guide provides comprehensive installation instructions for Git Provider Sync across different platforms and package managers.

== Downloading Git Provider Sync
1. Go to the https://github.com/itiquette/git-provider-sync/releases[Releases page] on GitHub.
2. Look for the latest version.
3. Download the file that matches your computer type (we'll help you choose in the next section).

== Installation Methods

=== For Linux Users
Choose the option that matches your Linux system:

1. *Using package managers (easier method)*:
   - For Ubuntu or Debian-like systems: Download the `.deb` file and install it with:
+
[source,console]
----
sudo apt install ./path-to-downloaded-file.deb
----
   - For Fedora or Red Hat-like systems: Download the `.rpm` file and install it with:
+
[source,console]
----
sudo dnf install ./path-to-downloaded-file.rpm
----
   - For Alpine Linux: Download the `.apk` file and install it with:
+
[source,console]
----
apk add --allow-untrusted ./path-to-downloaded-file.apk
----

2. *Using pre-compiled binaries*:
   - Download the appropriate `.tar.gz` file:
     * `linux_amd64.tar.gz` for Intel/AMD 64-bit systems
     * `linux_arm64.tar.gz` for ARM 64-bit systems (Raspberry Pi, Apple Silicon, etc.)
   - Extract and install:
+
[source,console]
----
tar -xzf gitprovidersync_*_linux_amd64.tar.gz
sudo mv gitprovidersync /usr/local/bin/
----
   - Verify installation: `gitprovidersync --version`

=== For BSD Users
1. Choose the appropriate file:
   - FreeBSD: `freebsd_amd64.tar.gz` or `freebsd_arm64.tar.gz`
   - OpenBSD: `openbsd_amd64.tar.gz` or `openbsd_arm64.tar.gz`
   - NetBSD: `netbsd_amd64.tar.gz` or `netbsd_arm64.tar.gz`

2. Extract and install:
+
[source,console]
----
tar -xzf gitprovidersync_*_*bsd_*.tar.gz
sudo mv gitprovidersync /usr/local/bin/
----

3. Verify installation: `gitprovidersync --version`


=== Using Docker or Podman
If you're familiar with Docker or Podman, you can run Git Provider Sync without installing it directly:

[source,console]
----
podman run --rm -it \
  -v ${PWD}/gitprovidersync.yaml:/usr/bin/gitprovidersync.yaml \
  ghcr.io/itiquette/gitprovidersync:<version> sync \
  --config-file /usr/bin/gitprovidersync.yaml
----

=== Build From Source

For those who prefer to build from source:

1. Download the source code:
   - `gitprovidersync-src.tar.gz` (preferred)
   - Or use Git: `git clone https://github.com/itiquette/git-provider-sync.git`

2. Build requirements:
   - Go 1.25 or later
   - Just build tool

3. Build and install:
+
[source,console]
----
tar -xzf gitprovidersync-src.tar.gz
cd git-provider-sync
just build
sudo cp bin/gitprovidersync /usr/local/bin/
----


== Verifying Your Download
To make sure your download is safe and hasn't been tampered with:

1. Download the `gitprovidersync_checksums_sha256.txt` file.
2. Use a checksum tool to verify that the file you downloaded matches the checksum in this file.

== Security Verification

For advanced security features and detailed verification methods, see link:docs/security-features.md[Security Features Guide].

[appendix]
== Advanced Installation Methods


=== Package Managers

Additional package managers:

- Currently only Linux packages are supported
- Manual installation available for other platforms  
- **Snap** (Linux): Planned

=== Container Options

**Podman**: Alternative to Docker
[source,console]
----
podman run --rm ghcr.io/itiquette/gitprovidersync:latest --help
----

**Kubernetes**: See link:docs/ci-examples.md[CI Examples] for CronJob deployment patterns.

